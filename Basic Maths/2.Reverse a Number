#include <bits/stdc++.h>
using namespace std;

//Time Complexity = O(log n)
void reverse(int n){
    int ans=0;
    while(n>0){
        ans  = ans*10 + n%10;
        n = n/10;
    }
    
    cout<<ans<<endl;
}


int main() {
    
    int n;
    cin>>n;
    
    reverse(n);

    return 0;
}
