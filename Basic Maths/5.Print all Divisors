#include <bits/stdc++.h>
using namespace std;

/*
first for loop has tc = O(√n) ,sort function has tc = O(√n · log n) and 2nd for loop has tc = O(√n). therefore tc = O(√n) +O(√n · log n)+ O(√n) = O(√n · log n)
*/
void Divisors(int n){
    vector<int> v;
    
    for(int i=1;i*i<=n;i++){ //i<=sqrt(n); this will take time
        if(n%i == 0){
            v.push_back(i);
            if(n/i != i){
                v.push_back(n/i);
            }
        }
    }
    sort(v.begin(),v.end());
    for(auto i:v){
        cout<<i<<" ";
    }
    
}


int main() {
    
    int n;
    cin>>n;
    
    Divisors(n);

    return 0;
}
